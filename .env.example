# Optional Persistent MongoDB Atlas Database Connection
# If left blank, the app will run using local JSON file / memory storage.
# For multi-user hosting (Vercel, Render, Railway, etc.), add your free MongoDB Atlas connection URI below.
MONGODB_URI=mongodb+sandbox:password@cluster0.mongodb.net/academic_hub?retryWrites=true&w=majority
PORT=3000
